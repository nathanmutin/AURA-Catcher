{
    "name": "aura-catcher-root",
    "private": true,
    "scripts": {
        "install:all": "npm install && npm install --prefix frontend && npm install --prefix backend",
        "dev": "concurrently \"npm run dev --prefix shared\" \"npm run dev --prefix frontend\" \"npm run dev --prefix backend\"",
        "build": "npm run build --prefix frontend && npm run build --prefix backend",
        "lint": "eslint ."
    },
    "devDependencies": {
        "@eslint/js": "^9.39.2",
        "concurrently": "^9.1.0",
        "eslint": "^9.39.2",
        "typescript-eslint": "^8.51.0"
    },
    "workspaces": [
        "frontend",
        "backend",
        "shared"
    ]
}