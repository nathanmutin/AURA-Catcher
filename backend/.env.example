DB_ROOT_PASSWORD=root_password
DB_USER=aura_catcher
DB_PASSWORD=aura_catcher_password
DB_NAME=aura_catcher

# Vérification de pseudo par email (voir README)
RESEND_API_KEY=re_your_api_key
# onboarding@resend.dev fonctionne pour tester (envoie uniquement à l'adresse
# de ton propre compte Resend) — remplace par une adresse sur un domaine
# vérifié pour envoyer à de vrais utilisateurs.
EMAIL_FROM=onboarding@resend.dev
# URL publique du site, utilisée dans le lien de vérification envoyé par
# email. Doit être joignable depuis internet en production (jamais localhost).
PUBLIC_URL=http://localhost:5173